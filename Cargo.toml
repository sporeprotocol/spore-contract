[workspace]
members = ["contracts/cellular", "lib/types", "lib/utils", "contracts/group"]
exclude = ["tests"]

[profile.release]
overflow-checks = true
strip = true
opt-level = 's'
lto = false
codegen-units = 1
